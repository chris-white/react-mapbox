/* OPTIONAL: Selectors are a useful design pattern suggested by react-redux, they can be used to transform the state of the store to a filter / manipulate the structure
* of the data returned without modifying the store itself. */
